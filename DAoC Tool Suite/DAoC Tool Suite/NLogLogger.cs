using NLog;

namespace DAoCToolSuite
{
    public static class NLogLogger
    {
        //private static readonly Logger logger1 = 
        public static Logger logger = LogManager.GetCurrentClassLogger();
    }
}